{{ form_start(commentForm) }}
    <div class="mt-3">
        <h5>Ajouter un Commentaire</h5>
        {{ form_widget(commentForm) }}
        <button class="btn btn-outline-primary">Commenter</button>
    </div>
{{ form_end(commentForm) }}
